## Flutter v1.0.19 — 恢复 2026copy.com 备用源线与网络更新

### ⚡ 线路与域名更新

- **新增与恢复有效源**：
  - 恢复 `2026copy.com`（`https://www.2026copy.com`，大陆备用）。
  - 保留 `copy3000.com`（`https://www.copy3000.com`，大陆推荐）。
  - 保留 `copy4000.com`（`https://www.copy4000.com`，大陆无障碍）。
  - 保留 `mangacopy.com`（`https://www.mangacopy.com`，国际线路）。
- **移除失效源**：
  - 移除重置/失效的 `copymanga.site` 备用线路。

### 资产
- Android：`CopyManga-flutter-1.0.19-{abi}.apk`（split-per-abi，推荐 `arm64-v8a`）
- iOS：`CopyManga-flutter-1.0.19-unsigned.ipa`（Actions 自动构建，需 Sideloadly 等重签侧载）

### 版本
`1.0.19+20`
