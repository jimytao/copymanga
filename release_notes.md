## Flutter v1.0.18 — 修复 v1.0.17 条漫高度锁定引入的两处缺陷

> **建议直接从 v1.0.16 升到本版，跳过 v1.0.17。**
> v1.0.17 修复条漫回跳的方向是对的，但实现带了两个缺陷，反而会削弱它自己的效果、
> 并且会裁切已下载章节的图片。本版全部修好。

### 修复一：条漫画面被打断（v1.0.17 引入）

v1.0.17 为了记住每张图的原始尺寸，在图片控件初始化时挂了一个解码监听。
问题是：**图片如果已经在内存缓存里，这个监听会立刻同步回调**，
而初始化本身发生在列表构建过程中——于是「构建期间又请求重新构建」，抛出异常并打断当前帧。

由于预载会主动把后几张图放进内存缓存，这条路径在条漫里几乎必然被高频触发，
等于 v1.0.17 一边修回跳、一边自己制造新的卡顿。

**修复**：构建期间的重建请求改为延到当前帧结束后执行；
同时把一帧内多张图的尺寸上报合并成一次重建（此前每张图都会触发一次整页重建）。

### 修复二：条漫图片被裁掉下半截（v1.0.17 引入）

v1.0.17 对还没测出比例的图片也强行按 3:4 锁定高度。
这个锁定是**硬性**的，比 3:4 更长的图会被直接裁掉下半部分。

影响最大的是**已下载到本地的章节**：它们走的是另一条代码分支，v1.0.17 没给它接尺寸探测，
比例永远学不到，于是条漫模式下**下载的漫画被永久裁成 3:4**。

**修复**：
- 比例还没测出来时**不做任何高度约束**（即 v1.0.17 之前的正常表现），测出真实比例后才锁定。
  这不会削弱防回跳效果——回跳来自被回收后重新构建的图片，而它们必然已经显示过一次、比例已知。
- 尺寸探测改为在线、离线共用一套机制，**下载的章节现在同样享受高度锁定**。

### 修复三：刚进章节时被凭空甩走一段（历史遗留问题）

打开章节后，断点续读要先读取本地存储，慢一点的机器上可能要几百毫秒。
此前这段时间里就算你已经开始往下滑了，读取一完成仍会无条件把你跳到上次的断点位置。

**修复**：检测到你已经用手指滑动过，就不再跳转，只提示一句「上次读到第 N 页」。

### 资产
- Android：`CopyManga-flutter-1.0.18-{abi}.apk`（split-per-abi，推荐 `arm64-v8a`）
- iOS：`CopyManga-flutter-1.0.18-unsigned.ipa`（Actions 自动构建，需 Sideloadly 等重签侧载，免费账号约 7 天需重签）

### 已知残留
条漫往回滑进「本次阅读从未构建过」的区域时，仍可能有小幅位置修正：
底层列表控件对没测量过的项目只能用平均高度估算。
宽高比缓存要等图片至少显示过一次才生效，因此这一段无法靠本次改动根治。

### 版本
`1.0.18+19`
