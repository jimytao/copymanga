边界二次确认换章对齐：点击 / 滑动 / 音量键共用确认态，并加固防误触。

## 新功能

### 📖 阅读器边界换章
- 首末页再次点击或按音量键：提示「再次按下加载上/下一章」，再操作一次才切章。
- 首末页越界滑动：提示「再次滑动加载上/下一章」；一次手指手势只计一次。
- 点击、滑动、音量键共用同一确认状态。
- 无邻章时第一次即提示「已经到头了~」。

### 🖱 点击分区
- 横向：左 / 中 / 右（右开本左右对调）。
- 纵向：上 / 中 / 下（中央菜单矩形，中带左右无效防误触）。
- 条漫：仍仅点按出菜单，避免滑动误触翻页。

## 修复 / 加固
- 从倒数第二页滑到末页的同一次手势，不再误弹「再次滑动」。
- 图片放大拖动时不触发越界换章。
- 音量键长按不再连跳确认态。
- 详情/章节顶栏返回优先 `history.back()`（i.js，与 Flutter 同源）。
