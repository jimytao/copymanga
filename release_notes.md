切章时同步表 H5 阅读进度，预取命中也不再留下旧话记录。

## 修复

### 📖 阅读进度同步
- **预取命中**也会静默把可见 WebView 导航到目标章节，退出阅读器后详情/历史不再停在旧话。
- **无预取切章**优先直接 `loadUrl` 目标章，比单靠页面控件点击更稳。

### 🛡️ 竞态与稳健性
- 预取未完成时不会误清空状态；放弃预取后丢弃迟到的收图结果，避免与新收图双开阅读器。
- 进度同步期间抑制 `loadComic`，防止重复触发隐藏 WebView。

### 🌐 源站
- 备用源 `2025copy.com` 替换为 `www.copymanga.site`（与 Flutter 版对齐）。
