## Flutter v1.0.17 — 修复条漫滚动回跳与单页章节无法切章，新增缓存上限

### 问题一：条漫滑着滑着突然闪一下往回跳

条漫每张图的 item 没有任何高度约束，图片没加载完时占位符（一个 loading 圈）高度接近 0。
当图片被内存缓存淘汰、或 item 滑出预加载范围被回收后重建，item 会先塌成零高再弹回真实高度；
列表总高度随之抖动，滚动锚点被重新对齐，看上去就是「闪一下，往回跳一段，看过的内容要重新滑」。

低内存机型（如小米 6X 配第三方 ROM）症状最重：系统内存压力回调会整片清空图片缓存，
一次性把所有可见图退回占位状态。

**修复**
- 新增宽高比缓存，记住每张图的原始尺寸，item 外套 `AspectRatio`，
  占位符与真实图片共用同一高度——加载中、被回收、重新下载，item 高度全程不变。
- 内存图片缓存上限由默认 100MB 提到 320MB，条数压到 80。
  条漫长图单张解码后可达 20–40MB，默认值几张就撑爆 LRU，反把视口附近的图挤掉。
- 预载窗口由后 5 张收到后 3 张，避免预载本身制造内存压力。

> 首次阅读某一章时仍可能有一次轻微的高度校正（比例尚未学到），再读同一章即完全平滑。

### 问题二：条漫某章只有一页时，下滑无法切换章节（仅 Android）

章节只有一页且图片不满一屏时，列表可滚动范围为零。此时 Android 的滚动物理
（`ClampingScrollPhysics`）会直接吞掉拖拽，既不发滚动更新也不发越界通知，
而条漫的切章完全依赖这两类通知，于是彻底失灵。

**修复**：为条漫列表显式指定 `AlwaysScrollableScrollPhysics`，内容不满一屏时同样接受拖拽并发出越界通知。
iOS 的回弹物理本就不受此限制，因此该问题仅在 Android 出现。

### 新功能：图片缓存上限（设置 → 存储 → 缓存上限）

此前缓存只能查看和手动清理，没有上限，长期使用体积可能非常可观。

- 可选档位：256MB / 512MB / **1GB（默认）** / 2GB / 4GB / 不限制。
- 达到上限后，自动删除**最久没看过**的图片，一次削减到上限的 80%。
  选「一次削到水位线」而非「每存一张顶掉一张」，是为了不让目录扫描压在滑动路径上导致掉帧。
- 触发时机：冷启动一次、阅读过程中按需触发（最小间隔 45 秒）、修改设置后立即执行。
- **已下载到本地的漫画不受影响**，只清在线阅读的图片缓存。
- 图片缓存改用独立目录，「清理缓存」不会误伤其他数据；缓存大小显示支持 GB 单位。

### 资产
- Android：`CopyManga-flutter-1.0.17-{abi}.apk`（split-per-abi，推荐 `arm64-v8a`）
- iOS：`CopyManga-flutter-1.0.17-unsigned.ipa`（Actions 自动构建，需 Sideloadly 等重签侧载，免费账号约 7 天需重签）

### 版本
`1.0.17+18`
