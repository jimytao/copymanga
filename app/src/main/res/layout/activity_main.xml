<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="mainViewModel"
            type="top.fumiama.copymangaweb.activity.viewmodel.MainViewModel" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <top.fumiama.copymangaweb.view.JSWebView
            android:id="@+id/wh"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <top.fumiama.copymangaweb.view.JSWebView
            android:id="@+id/w"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <ProgressBar
            android:id="@+id/pw"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="0dp"
            android:layout_height="2dp"
            android:visibility="@{mainViewModel.progressVisibility}"
            app:animateProgress="@{mainViewModel.progress}"
            app:animationDuration="@{mainViewModel.progressDuration}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/fab"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginEnd="24dp"
            android:alpha="0.8"
            android:background="@drawable/rndbg_round"
            android:foreground="@drawable/ic_dl"
            android:foregroundGravity="center"
            android:onClick="onFabClicked"
            android:visibility="@{mainViewModel.fabVisibility}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="@+id/w"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="0.275"
            tools:visibility="visible" />

        <Button
            android:id="@+id/fab_settings"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginEnd="24dp"
            android:alpha="0.8"
            android:background="@drawable/rndbg_round"
            android:text="设置"
            android:textColor="@android:color/white"
            android:textSize="12sp"
            android:onClick="onSettingsFabClicked"
            android:visibility="@{mainViewModel.settingsFabVisibility}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="@+id/w"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="0.375"
            tools:visibility="gone" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
